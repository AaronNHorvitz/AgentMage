# Host Shell

This Rust module remains a scaffold. It will compose the kernel with one
independently admitted aggregate platform adapter and expose only the
authenticated, bounded local protocol. The Phase 8 Linux mechanisms are not yet
wired into a user-facing host workflow.
