# Capability Packs

Capability packs implement bounded user-facing behavior through kernel contracts.
They cannot mint grants, redefine policy, contact a raw model runtime, or bypass
platform enforcement.
