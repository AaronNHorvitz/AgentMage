# Decision 0070: Coding Development Identity Prerequisites

| Field | Value |
|---|---|
| Status | Accepted under owner delegation, 2026-09-20 |
| Date | 2026-09-23 |
| Authority | Decision 0054 and the owner's real-model integration assignment |
| Scope | Development runtime answer identity, controlled-create observations and diagnostic pins |
| Preserves | Existing evidence validators, exact artifact/runtime hashes, 32K/4096 profile budgets, production activation, grants, drift checks and verifier ownership |

## Runtime identity

Muse native-3 performed a native hash, genuine failing validation, approved syntax
repair, passing validation, diff and status, then emitted a complete completion
candidate. The host failed during finalization. Its exact runtime build label
contained spaces; model admission accepts that text, but answer-evidence identity
validation rejects spaces. A local regression reproduces the rejection.

Keep that strict validator. Represent the two development candidates' exact build
as `llama.cpp-b10423-a94d563ed801d1da1b8c2432946de07d0231bb3d`. This preserves the
version, source commit and actual executable digest and produces new exact profile
manifest identities. The existing 8K demo profiles and separate service are not
changed. The failed run remains failed; only a fresh actual-model run can succeed.

## Creation prerequisite

The scripted new-file fixture privately computed the controlled-create parent
digest, but native models could not obtain it from their context. During existing
development workspace inspection, observe only the authorized writable-root
parents through the held Linux boundary and present their initial sibling-set
digests as source-bound data. Do not expose a grant or supply file contents or a
solution. The unchanged create boundary must rebind the exact current parent and
siblings, require an absent destination and obtain a fresh exact grant. Drift
refuses; the initial observation never authorizes creation or overwriting.

## Diagnostics

Retain source commit, tracked-diff digest, dirty paths, exact command, wrapper and
all three binary identities, elapsed time and post-run binary comparison in new
wrapper run records. Closed content-free coordinator error variants are retained
without arguments, output or credentials. These measurements are evidence, not
admission or independent review.
