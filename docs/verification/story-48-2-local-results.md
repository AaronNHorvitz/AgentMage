# Story 48.2 Local Verification Results

| Field | Result |
|---|---|
| Scope | Native coding profile, shared-runtime client, and four exact MVP fixture groups |
| Local source-level result | Pass with one declared model-admission prerequisite |
| `M-HARNESS-MVP` result | Blocked |
| Complete Story 48.2 result | Blocked |
| Sprint 48 result | Blocked |
| Release approval | No |

## Verified Locally

- `CodingSessionProfile` binds one repository snapshot, immutable base, owned
  worktree, admitted-model tuple, offline proof, native catalog, writable roots,
  instruction ledger, plan, commands, validations, limits, and 17 explicit MVP
  prohibitions into one canonical digest.
- Repository exploration, file reading, filesystem/text/code search, structured
  patching, controlled creation, bounded commands, targeted validation, and Git
  status/diff/log/show use the common native registry and dispatcher. No native
  coding tool requires MCP or a generic shell.
- The reusable coordinator and `drive_coding_client` complete one fake-model
  patch, test, Git inspection, and verifier-backed terminal path through an exact
  protected approval boundary. Denial and cancellation start no hidden effect.
- Repository instructions remain cited untrusted evidence. Accepted guidance can
  only remove tools, narrow writable roots and budgets, strengthen validation, or
  stop the run.
- The exact `S-048-MVP-E2E` fixture group passes every fake-model case. Its only
  unavailable case is `admitted-local-model-workflow`, which names the explicit
  `sprint-49-exact-runtime-admission` prerequisite and has no fabricated test.
- All 12 `S-048-MVP-STALE`, 12 `S-048-MVP-ADVERSARIAL`, and 14
  `S-048-MVP-ABSENCE` cases name real Rust tests. Those tests pass locally,
  including the ignored-by-default Linux fixed-environment and network-denial
  isolation test when invoked explicitly on the current Fedora host.
- Durable runtime composition can publish large model and tool payloads as
  content-addressed artifacts, bind their references to checkpoints, and resume
  one safe continuation without replaying a completed effect. Those later
  capabilities remain absent from the ephemeral MVP profile.

## Focused Commands

```text
python3 -m unittest tests.test_story_48_coding_harness_corpus
cargo test -p agentmage-host story_48_2 --lib --locked
cargo test -p agentmage-host s_048_mvp --lib --locked
cargo test -p agentmage-host --lib --locked
cargo test -p agentmage-kernel-engine --lib --locked
cargo test -p agentmage-platform-linux worker_receives_only_the_fixed_environment_and_no_network --lib --locked -- --ignored --nocapture
npm run docs:check
```

## Open Evidence

- No exact local model profile is currently admitted for product coding. The
  public executable therefore fails closed instead of substituting a fake,
  historical, unmeasured, or arbitrary endpoint.
- `agentmage code` selects the public command, but its installed stdin/history
  loop and authenticated host transport are not composed. Native Chat also does
  not yet consume the shared runtime protocol.
- The terminal presentation lacks the complete target/argument/preimage/limits
  approval view, every-boundary cancellation handling, and the full objective,
  diff, checks, risks, rollback, receipts, and evidence summary.
- Story 22.2 still owns complete artifact publication, generated-file handling,
  retention, collection, corruption, recovery, and pressure evidence.
- Crash, timeout, artifact-failure, every-event-phase cancellation, descendant
  cleanup, cross-interface parity, installed-package, supported-platform,
  performance, pressure, canary, and independent-review campaigns remain open.
- Manual fuzzing remains deliberately deferred to the final campaign.

These absences keep `M-HARNESS-MVP`, Story 48.2, Sprint 48, and dependent release
gates blocked. Passing source-level fixtures does not enable a model, installed
coding interface, supported platform, or release.
