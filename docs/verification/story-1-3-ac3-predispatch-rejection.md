# Story 1.3 AC3 pre-dispatch rejection

Story acceptance criterion `1.3.AC3` passes for the current canonical record-admission boundary.
The shared synthetic corpus proves that 45 missing, extra, malformed, oversized, and unsupported
records fail before canonical publication. The structurally valid cyclic workflow and individually
valid stale request binding fail at their exact trusted semantic and record-set boundaries.

The boundary returns stable typed codes. Shared serialization tests prove malformed input does not
enter the fixed diagnostic message, unsupported versions use `contract.version.unsupported`, and
the closed terminal diagnostic cannot carry model prose, prompt text, credentials, environment
values, or another free-form message. Admission and validation are pure pre-dispatch operations;
the evidence commands execute no model inference, tool worker, effect driver, or network call.

This closes the current local Rust/JSON record-admission criterion. It does not claim an installed
client or worker, native-platform execution, real-model execution, independent review, Story,
Sprint, packaging, or release completion.
