# Story 49.2 Alternate Runtime Evaluation

## Scope

The retained matrix inventories llama.cpp `b10333` plus unresolved Ollama,
LM Studio, vLLM, SGLang, TGI, Ray Serve LLM, KServe, OpenAI
Responses-compatible, and Anthropic Messages-compatible candidate families.
An unresolved family is not an implementation, version, endpoint, codec,
profile, platform, or support claim.

All candidates use one canonical ten-dimension semantic inventory: message
parts, structured output, tool proposals, stream events, usage, cancellation,
typed errors, health, concurrency, and version behavior. The unavailable
candidates mark every dimension unavailable. The pinned llama.cpp package
candidate marks every live semantic unverified because its product inference
composition and live parity campaign remain absent.

## Deterministic Contract Results

- Eight fake-adapter cases cover field preservation, ordering, backpressure,
  cancellation, malformed events, resource limits, typed error mapping, and
  version-change staleness through the canonical gateway contract.
- Every candidate has an explicit unavailable or unqualified disposition,
  gaps, remediation, false platform qualification, false support, and false
  route state.
- Version, codec, or endpoint identity change invalidates prior evidence.
- The applicable local `RV-54` identity and no-silent-fallback slice passes.
  Live performance remains explicitly unavailable.
- The Rust evaluator accepts the same complete evidence tuple for every
  planned family and grants no activation authority; a missing parity gate
  produces a rejected disposition.

## Truthful Disposition

The matrix proves a closed non-activating evaluation contract. It does not
prove a named adapter implementation, live runtime, model, endpoint, semantic
parity, performance, lifecycle, removal, platform, route, or fallback. There
are zero supported adapters, zero enabled routes, zero live candidates, and no
silent fallback. Any future exact candidate requires its own untouched live
evidence before configuration or routing.
