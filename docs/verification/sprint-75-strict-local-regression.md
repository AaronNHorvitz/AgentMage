# Sprint 75 Strict-Local Regression Status

The connected modules have no executor or configuration registration and strict-local remains the
default. Existing repository-wide local gates through Sprint 74 continue to pass up to the retained
Story 6.1 rootless-Podman host prerequisite. The Sprint 70 terminal contract restores strict-local
and requires fresh authority; later provider modules contain only pure projections.

The required full v0.1–v0.6 native rerun and 60-minute egress observation with connector absent and
disabled have not run in this sandbox. This artifact is a reviewable partial proof and preserves
that external blocker; it does not close S-062-I03 or S-062-AT01.
