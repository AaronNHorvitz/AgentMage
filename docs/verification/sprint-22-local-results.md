# Sprint 22 Local Verification Results

| Field | Result |
|---|---|
| Gate | Sprint 22 |
| Platform-neutral core | Pass |
| Sprint result | Blocked |
| Release approval | No |

## Verified Locally

- Context composition is deterministic at empty, nominal, boundary, and over-limit sizes, with
  visible omissions and fail-closed preservation of essential current intent.
- Authoritative evidence wins exact-source deduplication before lower-value summary or memory
  material.
- Denied and stale excerpts do not enter model context; a restricted secret canary is absent from
  both packet content and content-free debug accounting.
- Checked summaries preserve the required continuity fields but remain distinct from source
  evidence; stale, disputed, and insufficient summaries require reopening original sources.
- Safe-boundary checkpoints bind every required workspace, repository, instruction, permission,
  policy, model, evidence, action, grant, receipt, and next-action identity without source content or
  authority.
- Operational-store schema 4 publishes the terminal authority state and next checkpoint in one
  encrypted transaction, verifies the complete checkpoint history on open, and rejects tampering.
- Ephemeral mode persists no session checkpoint.
- All nine material drift dimensions are detected before another action and produce a complete
  explicit Continue, Restart, or Cancel decision boundary.
- The 126-run subprocess campaign covers before and after each of nine persistence boundaries;
  session-checkpoint recovery retains exactly one record and repeats no completed transition.
- The 100-run native Linux coordinator campaign exits without unwinding 25 times on each side of
  the tool-terminal event and checkpoint transactions; every reopen retains one worker launch,
  exposes no pre-commit terminal or checkpoint state, and repeats no completed effect.
- The production Linux coordinator resumes its longest fixture-safe prefix of seven tool turns,
  receipts, checkpoints, and continuation artifacts. Seven repository, policy, workspace,
  configuration, and exact-model drift classes block before action; exact state completes without
  another worker launch and preserves objective and history counts.
- The current product canary sweep composes eight passing source-bound checks across context input,
  persistence policy, encrypted store/export/crash diagnostics, runtime projections, owner-bound
  artifact reads, model/tool events, native payload ciphertext, and external-telemetry closure. Its
  report and repository-root-redacted raw trace contain no private user data or raw canary value.
- A failed terminal checkpoint after worker completion poisons the runtime; restart reconciles one
  receipt, and replay launches no worker.
- The full local product gate passes with all pre-existing ignored live, native-platform, and
  large-model tests still visible.

## Open Evidence

The production VS Code Chat path does not yet compose all model context through this manager or
present its debug view. The native campaigns use deterministic model execution and exact runtime
records on the current Linux source tree; Ubuntu clean-image, Windows, macOS, installed real-model,
installed-package, process-memory, swap, SSD-remanence, filesystem-snapshot, external host
instrumentation, and independent Sprint 22 review evidence are not retained. The current
source-surface canary campaign must also be extended whenever a new content-bearing source becomes
reachable.

Sprint 22 therefore remains blocked despite the complete platform-neutral context and encrypted
checkpoint core. The machine-readable record is retained at
[`local-evidence-report.json`](../../artifacts/sprints/sprint-22/local-evidence-report.json).
