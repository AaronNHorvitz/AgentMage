# macOS Platform Adapter

This Swift module will implement the Apple Silicon App Sandbox, XPC, Keychain,
security-scoped bookmark, code-identity, and native IPC boundaries. Implementation
and execution remain `BLOCKED-MACOS` until performed on the required hardware.
