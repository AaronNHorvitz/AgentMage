name: Documentation (Local Execution Contract)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  local_execution_only:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - name: Explain local execution authority
        run: echo "Documentation validation is executed by the repository-owned local gate."
